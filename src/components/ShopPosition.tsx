import React from 'react'

import Map from 'components/Map'

/**
 * Shows the position of the shop on a map
 *
 * @returns {React.ReactElement}
 */
function ShopPosition(): React.ReactElement {
  return <Map />
}

export default ShopPosition
